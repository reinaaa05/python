# 九九の表を作成してみよう

def multiply(x, y):
    return x * y

for num in range(1, 10):
    for num1 in range(1,10):
        print(multiply(num1, num), end="")
        if num1 < 9:
            print(", ", end="")
    print("")
